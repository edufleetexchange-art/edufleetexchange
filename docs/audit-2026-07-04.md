# Full-system audit — 2026-07-04 (Fable 5)

Method: live Playwright user-flow tests on dev + two static-analysis passes
(UI dead code/bloat, server consistency). ✅ = fixed same day.

## Fixed in this audit
- ✅ Landing search dropped the query and mis-routed to vehicles → keyword routing + `?q=` carry
- ✅ `/admin/sales` sidebar link had no route (redirected home) → wired
- ✅ AdApprovals/CreateAd toasts used an unmounted toast system (never displayed) → sonner
- ✅ Zero code-splitting → 29 authed/admin pages lazy-loaded + vendor chunk (public bundle 471→375KB gzip)
- ✅ `PUT /accounts/me` 404 (server was PATCH-only; UI sends PUT) — **every profile update was broken**
- ✅ `updateJob` mass-assignment (whole `req.body` → could overwrite status/instituteId) → allowlist
- ✅ `createSupplier` allowed self-set `isVerified` → stripped
- ✅ PII console.log in adminController (logged name/email/role of created users)

## P1 — remaining live contract breaks (UI calls → no such route; fail-soft but broken features)
| UI call | Server reality |
|---|---|
| `GET /subscriptions/check/listing-visibility/${id}` (subscriptionEnforcement.ts:289) | POST `/check/listing-visibility`, no param |
| `POST /subscriptions/decrement/job-post-count` (:252) | route doesn't exist (only listing-count) |
| `GET /subscriptions/status` (:336) | doesn't exist |
| `GET /subscriptions/usage-stats` (:373) | server shape is `/user/:userId/usage` |
| `GET /vehicles/${id}/views` (vehicleService.ts:220) | doesn't exist |
| `GET /jobs/${jobId}/applications` (teacherService.ts:237) | doesn't exist |

## P2 — consistency/quality
- Response envelopes: 7 distinct shapes; `subscriptionController` returns bare arrays/objects and leaks
  raw `error.message` with no `success/code`. Standardize on `{success, data, code?}`, then remove the
  defensive fallback in `apiClient.ts:139`.
- `error.message` leakage across ~8 controllers + global handler (`index.ts:186-208`).
- 4 copy-pasted `ok()` helpers (roster/interview/consultant/placement controllers) → consolidate.
- Guest "Login to Apply" loses return-path (no `?redirect=` support on /login).
- Unknown routes silently redirect to `/` — consider a real 404 page.
- Landing hero collects a location that search ignores.
- Three toast systems (sonner primary, react-hot-toast in 4 files, orphaned radix use-toast) → consolidate on sonner.

## P3 — dead code (safe-delete list, verified by import-graph)
UI: `src/lib/api.ts` (+ drop `axios`), `pages/AdminDashboard.tsx`, `components/ApplicationTable.tsx`,
`components/InstituteJobApplications.tsx` (page version is the live one), `components/InterviewScheduler.tsx`,
`hooks/useApiCall.ts`, `types/profileGuards.ts`, `constants/routes.ts`, `constants/categories.ts`,
~26 unused shadcn primitives under `components/ui/` (incl. `from.tsx` typo file), then prune ~20 unused deps
(`@react-three/*`, `@dnd-kit/core`, `zod`, `react-hook-form`, `embla-carousel-react`, `cmdk`, `vaul`, …).
Server: `express-validator` + `zod` deps unused (no schema validation exists anywhere — see P2),
`middleware/personaAccessControl.ts` enforcement exports, `uploadFields/uploadDocuments/uploadSingleDocument`,
`generalApiLimiter`. Unused-but-keep: `POST /auth/refresh`.
Console.logs: 10 in UI (list in audit agent report), 3 remaining in server.

## Product-gap TODOs found in code
- Dashboard analytics counts hardcoded to 0 (Dashboard.tsx:114,235,462,942)
- TeacherDashboard profile fields disabled awaiting backend endpoint (127,434,486,507,526,542)
- InstituteTeacherSearch "Connect" flow blocked on issue 40 (434,620)
